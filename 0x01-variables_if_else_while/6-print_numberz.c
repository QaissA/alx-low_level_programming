#include<stdio.h>
/**
 * main - entry point
 *
 * Return: Always 0 (success)
 */
int void(main)
{
int number = 0;
while (number <= 9)
{
putchar(number + '0');
number++;
}
putchar('\n');
return (0);
}
