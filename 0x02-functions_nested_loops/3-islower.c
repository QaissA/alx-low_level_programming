#include "main.h"
/**
 * _islower - check letter if lower or upper case
 * @c : the character to check
 * Return: 1 if is lower case , 0 if is uppercase
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
